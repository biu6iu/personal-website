---
const NAV_ITEMS = [
  { label: 'ABOUT', path: 'about' },
  { label: 'PROJECTS', path: 'projects' },
  { label: 'CONTACT', path: 'contact' },
  { label: 'PHOTOGRAPHY', path: 'photography' },
];

const base = import.meta.env.BASE_URL;

const linkClass =
  'block p-2.5 max-xs:p-2 text-[12px] max-xs:text-[11px] no-underline text-ink rounded-lg ' +
  'transition-[color,background-color,transform] duration-300 ease-[ease] hover:text-ink-muted ' +
  'active:text-ink-muted active:scale-95';
---

<nav
  class="p-6.25 max-md:p-3.75 w-1/5 max-md:w-full h-[95%] max-md:h-auto
         box-border flex flex-col justify-between items-center
         sticky max-md:relative overflow-hidden"
>
  <h1 class="mt-5 text-[24px] max-md:text-[20px] font-normal">
    <a href={base} class="text-center no-underline text-ink">STEVEN LU</a>
  </h1>

  <div class="flex-1 flex flex-col justify-center items-center max-md:my-2.5">
    <ul
      class="list-none m-0 p-0 flex flex-col items-center
             max-md:flex-row max-md:justify-center max-md:flex-wrap max-md:gap-5 max-xs:gap-2.5"
    >
      {NAV_ITEMS.map((item) => (
        <li><a href={`${base}${item.path}`} class={linkClass}>{item.label}</a></li>
      ))}
      <li>
        <button id="theme-toggle" class={`${linkClass} bg-transparent border-0 cursor-pointer font-serif uppercase`}>
          Light Mode
        </button>
      </li>
    </ul>
  </div>
</nav>

<script>
  const themeToggle = document.getElementById('theme-toggle') as HTMLButtonElement;

  const setLabel = () => {
    const isDark = document.documentElement.classList.contains('dark-mode');
    themeToggle.textContent = isDark ? 'Dark Mode' : 'Light Mode';
  };

  setLabel();

  themeToggle.addEventListener('click', () => {
    document.documentElement.classList.toggle('dark-mode');
    setLabel();
    const isDark = document.documentElement.classList.contains('dark-mode');
    localStorage.setItem('theme', isDark ? 'dark' : 'light');
  });
</script>
